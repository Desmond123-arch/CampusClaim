server {
    listen 80;
    server_name api.campusclaim.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Waiting for certificate setup...';
        add_header Content-Type text/plain;
    }
}
